algoritmo "QUESTAO 55"
var
   i, resultado: inteiro
inicio
   // Laço de repetição para imprimir a tabuada do 8
   para i de 1 ate 10 faca
      // Calcula o resultado da multiplicação
      resultado <- 8 * i
      // Imprime a linha da tabuada
      escreva("8 x ", i, " = ", resultado, "\n")
   fimpara
fimalgoritmo
